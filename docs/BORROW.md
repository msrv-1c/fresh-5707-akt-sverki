```text
/cfe-borrow <ConfigPath> <src> Document.АктСверкиВзаиморасчетов
/cfe-patch-method ... // модуль формирования печати акта — After
```

В перехвате вызвать `ГСК5707_ОбщегоНазначения.ДополнитьШапкуАктаСверки`.
